$(function() {
    var input = $('form[name="Form"] input');
    for (var i = 0; i < input.length - 1; i++) {
        var placeholder = input.eq(i).attr("placeholder");
        if (placeholder < twoHoursFromNow) {
                    input.eq(i).val("×");
                    input.eq(i).attr('disabled', true);
        }
      }
    // {{--  ブッキングコントローラー特定の日付用  --}}1
    bookingController.forEach(function(item, index) {
      var dayOfTheWeek = item.day_of_the_week;
      for (var i = 0; i < document.Form.length - 1; i++) {
        var placeholder = input.eq(i).attr("placeholder");
        if (Math.floor(placeholder / 10000) == dayOfTheWeek) {
          input.eq(i).val("×");
          input.eq(i).attr('disabled', true);
        }
      }
    })
    // {{--  ブッキングコントローラー特定の時間帯用  --}}
    bookingController2.forEach(function(item, index) {
      var dayTime = item.day_time;
      for (var i = 0; i < document.Form.length - 1; i++) {
        var placeholder = input.eq(i).attr("placeholder");
        if (placeholder == dayTime) {
            for (var l = 0; l < 16; l += 1) {
                if ($('form[name="Form"] #length_of_time').val() === 1 + 0.5 * l + '時間') {
                  for (var j = 0; j < 14 * l + 15; j += 14) {
                    input.eq(i - j).val("×");
                    input.eq(i - j).attr('disabled', true);
                  }
                } else {
                  input.eq(i).val("×");
                  input.eq(i).attr('disabled', true);
                }
            }
        }
      }
    })
    // {{--  既存の予約用  --}}
    booking.forEach(function(item, index) {
      var bookingDate = item.booking_date_number;
      var time = item.length_of_time;
      for (var i = 0; i < document.Form.length - 1; i++) {
        if (input.eq(i).attr("placeholder") == bookingDate) {
          input.eq(i).prop("name", time)
          for (var l = 0; l < 16; l += 1) {
            if (input.eq(i).attr('name') === 1 + 0.5 * l + '時間') {
              for (var j = 0; j < 14 * l + 15; j += 14) {
                input.eq(i + j).val("×");
                input.eq(i + j).attr('disabled', true);
              }
            } else {
              input.eq(i).val("×");
              input.eq(i).attr('disabled', true);
            }
          }
          for (var m = 0; m < 16; m += 1) {
            if ($('form[name="Form"] #length_of_time').val() === 1 + 0.5 * m + '時間') {
                if(input.eq(i) == '×') {
                    for (var j = 0; j < 14 * m + 15; j += 14) {
                input.eq(i - j).val("×");
                input.eq(i - j).attr('disabled', true);
              }
                }
            } else {
              input.eq(i).val("×");
              input.eq(i).attr('disabled', true);
            }
        }
        }
      }
      for (var m = 0; m < 16; m += 1) {
        if ($('form[name="Form"] #length_of_time').val() === 1 + 0.5 * m + '時間' && input.eq(266).val() !== "〇" && input.eq(266).val() !== "×") {
          for (var k = 252-14 * m; k < 266; k++) {
              input.eq(k).val("×");
              input.eq(k).attr('disabled', true);
            }
        }
      }
    })
  })
  $(function() {
    var input2 = $('form[name="Form2"] input');
    for (var i = 0; i < document.Form2.length - 1; i++) {
        var placeholder = input2.eq(i).attr("placeholder");
        if (placeholder < twoHoursFromNow) {
                    input2.eq(i).val("×");
                    input2.eq(i).attr('disabled', true);
        }
      }
    // {{--  ブッキングコントローラー特定の日付用  --}}
    bookingController.forEach(function(item, index) {
      var dayOfTheWeek = item.day_of_the_week;
      for (var i = 0; i < document.Form2.length - 1; i++) {
        var placeholder = input2.eq(i).attr("placeholder");
        if (Math.floor(placeholder / 10000) == dayOfTheWeek) {
          input2.eq(i).val("×");
          input2.eq(i).attr('disabled', true);
        }
      }
    })
    // {{--  ブッキングコントローラー特定の時間帯用  --}}
    bookingController2.forEach(function(item, index) {
      var dayTime = item.day_time;
      for (var i = 0; i < document.Form2.length - 1; i++) {
        var placeholder = input2.eq(i).attr("placeholder");
        if (placeholder == dayTime) {
            for (var l = 0; l < 16; l += 1) {
                if ($('form[name="Form2"] #length_of_time').val() === 1 + 0.5 * l + '時間') {
                  for (var j = 0; j < 14 * l + 15; j += 14) {
                    input2.eq(i - j).val("×");
                    input2.eq(i - j).attr('disabled', true);
                  }
                } else {
                  input2.eq(i).val("×");
                  input2.eq(i).attr('disabled', true);
                }
            }
        }
      }
    })
    // {{--  既存の予約用  --}}
    booking.forEach(function(item, index) {
      var bookingDate = item.booking_date_number;
      var time = item.length_of_time;
      for (var i = 0; i < document.Form2.length - 1; i++) {
        if (input2.eq(i).attr("placeholder") == bookingDate) {
          input2.eq(i).prop("name", time)
          for (var l = 0; l < 16; l += 1) {
            if (input2.eq(i).attr('name') === 1 + 0.5 * l + '時間') {
              for (var j = 0; j < 14 * l + 15; j += 14) {
                input2.eq(i + j).val("×");
                input2.eq(i + j).attr('disabled', true);
              }
            } else {
              input2.eq(i).val("×");
              input2.eq(i).attr('disabled', true);
            }
          }
          for (var m = 0; m < 16; m += 1) {
            if ($('form[name="Form2"] #length_of_time').val() === 1 + 0.5 * m + '時間') {
              for (var j = 0; j < 14 * m + 15; j += 14) {
                input2.eq(i - j).val("×");
                input2.eq(i - j).attr('disabled', true);
              }
            } else {
              input2.eq(i).val("×");
              input2.eq(i).attr('disabled', true);
            }
        }
        }
      }
      for (var m = 0; m < 16; m += 1) {
        if ($('form[name="Form2"] #length_of_time').val() === 1 + 0.5 * m + '時間' && input2.eq(266).val() !== "〇" && input2.eq(266).val() !== "×") {
          for (var k = 252-14 * m; k < 266; k++) {
              input2.eq(k).val("×");
              input2.eq(k).attr('disabled', true);
            }
        }
      }
    })
  })
  // ↓カレンダー値別カラー↓
  $(function() {
        $('#tabBox1 input[value="×"]').css({
          'color': '#696969',
          'font-weight': '600'
        });
        $('#tabBox1 input[value="〇"]').css({
          'color': '#355392',
          'font-weight': '600'
        });
        $('#tabBox2 input[value="×"]').css({
          'color': '#696969',
          'font-weight': '600'
        });
        $('#tabBox2 input[value="〇"]').css({
          'color': '#d84b6a',
          'font-weight': '600'
        });
  });
  // ↑カレンダー値別カラー↑
  $(document).ready(function() {
    if (matchMedia('(max-width: 960px)').matches) {
      $('.global-nav__list').removeClass('delete');
      $('#media').show();
      $('.nav1').addClass('delete');
      $('.header li').removeClass('p-3');
    }
  });
  //   ↓タブレット、スマホとPCのリンク先を変更↓
  $(document).ready(function() {
    if (matchMedia('(max-width: 960px)').matches) {
        $('#booking-btn-2').removeClass('delete');
        $('#booking-btn-5').removeClass('delete');
    } else {
      $('#booking-btn-1').removeClass('delete');
      $('#booking-btn-4').removeClass('delete');
    }
  });
  //   ↑タブレット、スマホとPCのリンク先を変更↑
  $(document).ready(function() {
    if (matchMedia('(max-width: 960px)').matches) {
      $('#footer-pc').addClass('delete');
    } else {
      $('#footer-sm').addClass('delete');
    }
  });
  $(window).scroll(function() {
    // ↓スクロールで吹き出しが出るタイミング↓
    if ($(window).scrollTop() >= 200 && matchMedia('(max-width: 960px)').matches) {
      $('.balloon').removeClass('delete');
      $('.balloon').show(3000);
    }
    if ($(window).scrollTop() <= 500 && matchMedia('(max-width: 960px)').matches) {
      $('.balloon').removeClass('delete');
      $('.balloon').show(3000);
    }
    if (($(window).scrollTop() < 200 || $(window).scrollTop() > 500)) {
      $('.balloon').addClass('delete');
    }
    // ↑スクロールで吹き出しが出るタイミング↑
    //  ↓予約ボタンが定位置に戻るタイミング↓
    if ($(window).scrollTop() >= 900) {
      $('.booking-btn-container').css('position', 'static');
    }
    if ($(window).scrollTop() < 900) {
      $('.booking-btn-container').css('position', 'fixed');
    }
  });
  //    ↑予約ボタンが定位置に戻るタイミング↑
  // ↓ドロワー↓
  function toggleNav() {
    var body = document.body;
    var hamburger = document.getElementById('js-hamburger');
    var blackBg = document.getElementById('js-black-bg');
    hamburger.addEventListener('click', function() {
      body.classList.toggle('nav-open');
    });
    blackBg.addEventListener('click', function() {
      body.classList.remove('nav-open');
    });
  }
  toggleNav();
  //   ↑ドロワー↑
  //   ↓トグルスイッチ↓
  $(function() {
    $("#tabMenu li a").on("click", function() {
      $("#tabBoxes div").hide();
      $($(this).attr("href")).fadeToggle();
    });
    return false;
  });
  $(function() {
    $(".toggle-input").on("click", function() {
      if ($(".toggle-input").prop("checked") == true) {
        $("#tabBoxes div").hide();
        $($(".woman").attr("href")).fadeToggle();
      } else {
        $("#tabBoxes div").hide();
        $($(".man").attr("href")).fadeToggle();
      }
    });
    return false;
  });
  // ↑トグルスイッチ↑
  // ↓男性女性の名前をクリックしたときもトグルスイッチが動く設定↓
  $(function() {
    $(".woman").on("click", function() {
      $('input[type="checkbox"]').prop('checked', true);
    });
  });
  $(function() {
    $(".man").on("click", function() {
      $('input[type="checkbox"]').prop('checked', false);
    });
  });
  // ↑男性女性の名前をクリックしたときもトグルスイッチが動く設定↑




